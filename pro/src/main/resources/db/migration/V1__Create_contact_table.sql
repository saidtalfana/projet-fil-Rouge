CREATE TABLE Contact (
                         contact_id INT AUTO_INCREMENT PRIMARY KEY,
                         name VARCHAR(255),
                         email VARCHAR(255),
                         date DATE,
                         time TIME,
                         phone VARCHAR(255),
                         message TEXT
);
